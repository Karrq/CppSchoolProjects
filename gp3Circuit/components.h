#ifndef COMPONENTS_H
#define COMPONENTS_H

coords drawCapacitor(coords, bool=false);
coords drawResistor(coords, bool=false);
coords drawTransistor(coords, bool=false, int=0);
coords drawGround(coords);

#endif
